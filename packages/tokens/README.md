# @kijani/tokens

Shared design-token foundation for the Kijani design system — the single source of truth consumed by both `@kijani/web` and `@kijani/mobile`.

**Status:** placeholder. The token source + build (currently in `@kijani/web/src/tokens`) moves here in Phase 0 step 2, which also adds the React Native output (`tokens.native.ts`).
