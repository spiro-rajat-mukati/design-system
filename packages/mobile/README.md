# @kijani/mobile

Kijani's React Native (Expo) component library — a separate library from `@kijani/web`, built on the same `@kijani/tokens` foundation.

**Status:** placeholder. Scaffolded in Phase 0 step 3 (ThemeProvider + light/dark, wired to the RN token output), with the core component set following.
